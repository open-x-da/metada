// Stub source file for common_io library when BUFR support is disabled
// This ensures the library always has at least one source file

namespace metada::backends::io {
// Empty namespace to ensure the file compiles
// When BUFR is disabled, this provides the minimal source needed for the
// library
}
